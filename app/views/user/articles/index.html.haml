%table.table.table-striped
  %thead
    %tr
      %th= t 'articles.title'
      %th= t 'articles.index.text_sample'
      %th= t 'articles.index.actions'
  %tbody
    = render @articles.decorate
    = paginate @articles, :theme => 'twitter-bootstrap-3'
